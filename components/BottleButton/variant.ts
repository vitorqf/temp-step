import { StyleSheet } from "react-native";

export const Variant = StyleSheet.create({
  large:{
    maxWidth: '100%',
    width: '100%'
  },
})

export const VariantIcon = StyleSheet.create({
  large:{
    height: 50
  }
})